katok = [];

def add_data(friend):
    katok.append(None);
    kLen = len(katok)
    katok[kLen-1] = friend;

add_data('다현')
add_data('정연')
add_data('쯔위')
add_data('사나')
add_data('지효')
add_data('모모')
print(katok);


